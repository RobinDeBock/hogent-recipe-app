import '../support/commands';

describe('Recipe list', () => {
    beforeEach(() => {
        cy.prepTest();
        cy.login();

       // TODO: look up the correct command in the 'support/commands.js' file and programmatically add two recipes
    });

    it('should show a list of recipes', () => {
      // TODO: implement
    });

    it('should be possible to remove an recipe from the list', () => {
      // TODO: implement
    });
});
